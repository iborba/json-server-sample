const Chance = require('chance')

module.exports = function analysis() {
  const chance = new Chance()
  const data = { analysis: [] }

  for (let i = 1; i <= 10; i++) {
    data.analysis.push({

    })
  }

  return data;
}